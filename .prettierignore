dist
drizzle
.wrangler
node_modules
package-lock.json

# Docs are hand-formatted prose and tables; Prettier reflows them into noise.
*.md

# Lockfiles and package-manager config are managed by the tool, not by us.
pnpm-lock.yaml
pnpm-workspace.yaml
